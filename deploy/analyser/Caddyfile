# VST webview (loaded by the plugin itself)
analyser.hardwavestudios.com {
    encode gzip

    reverse_proxy localhost:3004 {
        header_up Host {host}
        header_up X-Real-IP {remote_host}
        header_up X-Forwarded-For {remote_host}
        header_up X-Forwarded-Proto {scheme}
    }
}

# Binary / release downloads
dl.hardwavestudios.com {
    encode gzip

    root * /opt/hardwave/releases

    # Directory listing off — direct links only
    file_server {
        hide .git
    }

    header {
        Content-Disposition "attachment"
        Cache-Control "no-cache"
    }
}
